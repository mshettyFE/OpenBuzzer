#ifndef PRIORITY_QUEUE
#define PRIORITY_QUEUE

#include "Constants.h"

class PriorityQueue {
// define array of Records
// 0th index is always 0,0 (ie invalid)
    Rec heap[MAX_DEVICES+1];
public:
    PriorityQueue();
    Insert();
    Parent(index  )
};

#endif